import { types } from '../types/types';

const initialState = {
  items: [],
  total: null,
};

export const cartReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
